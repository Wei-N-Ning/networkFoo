#!/usr/bin/env bash

# motivation:
# I have launched a couple of new Vmware vms, how can I ssh to
# them using their hostname (instead of ip addr)?

# source: https://unix.stackexchange.com/questions/55691/how-to-add-an-ip-to-hostname-file
# sudo vim /etc/hosts
# <ip addr> <hostname>
