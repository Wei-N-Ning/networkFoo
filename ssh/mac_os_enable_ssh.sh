#!/usr/bin/env bash

# I can also use sshd debugging mode to temporarily open a 2222 
# etc port for an one-off connection.

enable_ssh() {
    # system preference -> sharing -> remote 

    # add the superuser/myself

    # test it on another workstation in the same subnet
    :
}
